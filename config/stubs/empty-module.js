// Empty module stub — used by Metro when IS_DEV_MODE=true to prevent
// bundling failures for native modules not installed in node_modules
// (e.g. ffmpeg-kit-react-native, react-native-vision-camera).
//
// Actual mock implementations are loaded at runtime via config/modules.js.

module.exports = {}
